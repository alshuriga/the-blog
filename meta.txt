Entities

	Base Entity - Id
	
	Post - Header, Text, Tags, DateTime
	Commentary - Username, Email, Text, DateTime
	Tag - Name


DTO

	Post:
		PostListDTO - Id, Header, Text, Tags, CommentsCount, DateTime
		PostDTO - Header, Text, Tags, Comments(page), DateTime
		EditPostDTO - Id, Header, Text, TagString
		CreatePostDTO - Header, Text, TagString

	Commentary
		CommentaryDTO - Username, Email, Text, DateTime
		CreateCommentaryDTO - Text, PostId

	Tag
		TagDTO - Name


ViewModels

	PostsListVm:
		IEnumerable<Post>
		PaginationData
		 

CQRS

	Queries //make proper parameters
		ListPostsQuery (specification) => IEnumerable<PostListDTO>
		RetrievePostQuery (Id, specification) => PostDTO 


	Commands 
		CreatePostCommand (CreatePostDTO)
		DeletePostCommand(Id)
		UpdatePostCommand(EditPostDTO)

		CreateCommentaryCommand(CreateCommentaryDTO)
		DeleteCommentryCommand(Id)


Exceptions 
	NotFoundException
	ValidationException
